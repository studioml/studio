database:
    type: s3
    authentication: none
    aws_access_key: MountD00m
    aws_secret_key: 0rcsAndSpiders
    bucket: test-database
    endpoint: http://minio02.geneticfinance.com:9000/

env:
    AWS_ACCESS_KEY_ID: MountD00m
    AWS_DEFAULT_REGION: us-west-2
    AWS_SECRET_ACCESS_KEY: 0rcsAndSpiders

server:
    authentication: None

storage:
    type: s3
    aws_access_key: MountD00m
    aws_secret_key: 0rcsAndSpiders
    bucket: test-storage
    endpoint: http://minio02.geneticfinance.com:9000/

log:
    name: output.log

saveWorkspaceFrequency: 1m #how often is workspace being saved (minutes)
saveMetricsFrequency: 1m
verbose: error

cloud:
    queue:
        rmq: amqp://user:al9DdqRNfvTT@54.71.90.227:5672/%2f?connection_attempts=30&retry_delay=1&socket_timeout=15

resources_needed:
    cpus: 2
    ram:  3g
    hdd:  60g
    gpus: 0

