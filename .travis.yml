dist: trusty

language: python

python:
  - "2.7"

env:
  fast_finish: true

before_install:
  - env

install:
  - cp pypirc ~/.pypirc
  - sed -ri "s/\{\{.password\}\}/$PYPI_PASSWORD/" ~/.pypirc
  - chmod 0600 ~/.pypirc
  - pip install -r requirements.txt

script:
  - python setup.py sdist upload
